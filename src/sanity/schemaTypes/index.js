import {blockContentType} from './blockContentType'
import {pageType} from './pageType'
import { postType } from './postType'

export const schema = {
  types: [blockContentType, pageType, postType ],
}
