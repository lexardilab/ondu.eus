/** @type {import('next').NextConfig} */
const nextConfig = {
  i18n: {
    locales: ["es", "eu"], // Idiomas disponibles: castellano y euskera
    defaultLocale: "es", // Idioma por defecto
  },
};

export default nextConfig;
